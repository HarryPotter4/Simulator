<UserControl x:Class="Simulation.Views.OperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Simulation.Views"
             mc:Ignorable="d" 
             >
    <Grid Height="324" Width="538" >
        <DataGrid ItemsSource="{Binding DataGrid_Operation}" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="539"   Margin="0,0,-1,0" IsEnabled="True" Height="324" RenderTransformOrigin="0.5,0.517">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn  Binding="{Binding CheckBox_Breakpoint}"  ClipboardContentBinding="{x:Null}"  Header="Breakpoint"/>
                <DataGridTextColumn  Binding="{Binding Text_Line}" ClipboardContentBinding="{x:Null}" Header="Line"/>
                <DataGridTextColumn  Binding="{Binding Text_ProgramCounter}" ClipboardContentBinding="{x:Null}" Header="Program Counter"/>
                <DataGridTextColumn Binding="{Binding Text_Operation}" ClipboardContentBinding="{x:Null}" Header="Operation"/>
                <DataGridTextColumn Binding="{Binding Text_SourceCode}" ClipboardContentBinding="{x:Null}" Header="Source Code"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
