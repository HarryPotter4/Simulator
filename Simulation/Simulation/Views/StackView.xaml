<UserControl x:Class="Simulation.Views.StackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Simulation.Views"
             mc:Ignorable="d" >


    <Grid>
        <DataGrid ItemsSource="{Binding DataGrid_StackView}" CanUserAddRows="False" VerticalAlignment="Top"   IsEnabled="True" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Column_Stacknumber}"  Header="Stack" />
                <DataGridTextColumn Binding="{Binding Column_Value}" Header="PC" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
